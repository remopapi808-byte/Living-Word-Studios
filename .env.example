# Copy to .env.local and fill in. Deploys receive actual values from the platform.
DATABASE_URL=postgresql://user:password@localhost:5432/living_word_studios
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Optional: used by proxy.ts CSP connect-src when set.
# NEXT_PUBLIC_API_URL=
